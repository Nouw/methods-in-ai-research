[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["methods_in_ai_research"]

[project]
name = "methods-in-ai-research"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.3.3",
    "scikit-learn>=1.7.2",
    "torch>=2.14.0",
    "transformers>=5.17.0",
]
